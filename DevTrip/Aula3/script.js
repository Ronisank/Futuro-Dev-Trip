// function mensagem(nome) {
//     document.write(`Convido ${nome} para o casamento de Juriscreide e Kleber`);
// }
// var nome = prompt("Digite seu nome");

// mensagem(nome)
for (let i = 0; i < 5; i++) {
    document.write(" contador: " + i);
}
